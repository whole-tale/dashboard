
import { fileIconFor } from 'wholetale/helpers/file-icon-for';
import { module, test } from 'qunit';

module('Unit | Helper | file icon for');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fileIconFor([42]);
  assert.ok(result);
});

