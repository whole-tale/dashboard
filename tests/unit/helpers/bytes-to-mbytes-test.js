
import { bytesToMBytes } from 'wholetale/helpers/bytes-to-mbytes';
import { module, test } from 'qunit';

module('Unit | Helper | bytes to mbytes');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bytesToMBytes([42]);
  assert.ok(result);
});

