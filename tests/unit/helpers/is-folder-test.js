
import { isFolder } from 'wholetale/helpers/is-folder';
import { module, test } from 'qunit';

module('Unit | Helper | is folder');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isFolder([42]);
  assert.ok(result);
});

