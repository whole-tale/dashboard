
import { ucfirst } from 'wholetale/helpers/ucfirst';
import { module, test } from 'qunit';

module('Unit | Helper | ucfirst');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ucfirst([42]);
  assert.ok(result);
});

