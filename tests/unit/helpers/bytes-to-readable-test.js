
import { bytesToReadable } from 'wholetale/helpers/bytes-to-readable';
import { module, test } from 'qunit';

module('Unit | Helper | bytes to readable');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = bytesToReadable([42]);
  assert.ok(result);
});

